# 코드 패턴

> ⬜ 기술스택 확정 후 채울 것.
> 아래 섹션 제목은 유지하고 실제 코드 예시만 채우면 됨.

---

## API 응답 표준 패턴

> 성공/실패 응답 형식, 에러 코드 규칙

---

## API Route 기본 패턴

> GET(목록), GET(단건), POST(생성), PATCH(수정), DELETE(삭제) 각각의 boilerplate

---

## 인증 / 권한 체크 패턴

> API route에서 로그인 여부, 프로젝트 멤버 여부, 역할 체크하는 방법

---

## 데이터 페칭 패턴 (클라이언트)

> useQuery, useMutation 사용 방법, queryKey 네이밍 규칙

---

## 낙관적 업데이트 패턴

> 목록에서 즉시 반영 후 서버 동기화

---

## 폼 처리 패턴

> 폼 라이브러리 사용법, validation, 에러 표시

---

## 목록 페이지 패턴

> 필터, 검색, 정렬, 페이지네이션 포함한 표준 목록 화면 구조

---

## 상세/편집 패턴

> 마스터-디테일 레이아웃, 인라인 편집, 저장 즉시 반영

---

## 트리 컴포넌트 패턴

> 계층 데이터 펼치기/접기, 드래그앤드롭 순서 변경

---

## 드래그앤드롭 순서 변경 패턴

> sortOrder 즉시 저장 방식

---

## 파일 업로드 패턴

---

## 마크다운 에디터 패턴

---

## AI 태스크 요청 / 결과 표시 패턴

> 요청 버튼 클릭 → 폴링으로 상태 확인 → 결과 인라인 표시

---

## 토스트 / 알림 패턴

---

## 에러 처리 패턴

> API 에러, 네트워크 에러, 폼 에러 각각의 처리 방법
