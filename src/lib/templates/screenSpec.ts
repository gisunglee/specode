/**
 * screenSpec.ts — 화면 설명(spec) 기본 템플릿
 *
 * 화면 상세 페이지에서 spec 컬럼이 비어있을 때 이 텍스트를 기본값으로 사용합니다.
 * 내용 수정이 필요하면 이 파일만 편집하면 됩니다.
 */

export const DEFAULT_SCREEN_SPEC = `## 화면설명

**비즈니스 목적**
(이 화면이 실제로 해결해야 하는 업무를 한 문단으로 구체적으로 작성)

**영역 목록**
(레이아웃 빌더에서 구성할 구조를 간단히 요약. 예: 행 1 - 검색+필터 영역 / 행 2 - 데이터 그리드)

**특이 UI 요구사항**
- 웹표준/웹호환성 준수
- 접근성 요구사항 (키보드 네비게이션, 스크린리더, 색맹 대비 등)
- 반응형 지원
`;
